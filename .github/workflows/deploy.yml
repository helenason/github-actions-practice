name: Github Actions 실행시키기

on:
  push:
    branches:
      - main

jobs:
  Deploy:
    runs-on: ubuntu-latest
#    runs-on: self-hosted
    steps:
#      - name: SSH로 EC2에 원격 접속
#        uses: appleboy/ssh-action@v1.0.3
#        env:
#          APPLICATION_PROPERTIES: ${{ secrets.APPLICATION_PROPERTIES }}
#        with:
#          host: ${{ secrets.EC2_HOST }}
#          username: ${{ secrets.EC2_USERNAME }}
#          key: ${{ secrets.EC2_PRIVATE_KEY }}
#          envs: APPLICATION_PROPERTIES
#          script_stop: true
#          script: |
#            cd /home/ubuntu/github-actions-practice
#            rm -rf src/main/resources/application.yml
#            git pull origin main
#            echo "$APPLICATION_PROPERTIES" > src/main/resources/application.yml
#            ./gradlew clean build
#            sudo fuser -k -n tcp 8080 || true # 서버 없어서 에러 뜨면 true
#            nohup java -jar build/libs/*SNAPSHOT.jar > ./output.log 2>&1 &
      - name: SSH로 EC2에 원격 접속 (without appleboy)
        run: |
          ssh -t -i ${{ secrets.EC2_KEY_PATH }} ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }}
